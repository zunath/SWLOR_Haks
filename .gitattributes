*.set text eol=crlf
/sw_portrait/*.dds binary
/sw_portrait/*.txi text eol=lf

# DDS texture resources are binary in every HAK.
*.dds binary

# Keep hash-pinned conversion manifests stable on Windows checkouts.
/tools/WorldTextureDds*.json text eol=lf
